name: kube-helm
template: ./kube-helm/
kind: Stack
disabled: false
backend: default
variables:
  k8s_conf:
    config_path: "~/.kube/config"
    config_context: "k3d-test"
  consul:
    name_override: renamed-consul
  nginx:
    name_override: renamed-nginx


