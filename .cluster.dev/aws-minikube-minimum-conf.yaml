installed: false

name: dev-minikube-minimum-conf

provider:
  type: aws
  region: eu-central-1

  provisioner:
    type: minikube

#
# Path to apps that will be managed in GitOps way with ArgoCD.
#
apps:
  - /kubernetes/apps/samples
